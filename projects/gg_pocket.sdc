#
# Project-level constraints.
#
# Everything this core needs is in platform/pocket/apf_constraints.sdc, which
# Analogue provides and which reads target/pocket/core_constraints.sdc after
# derive_pll_clocks. This file exists so the project has an SDC of its own to
# put anything project-wide in, and is deliberately empty.
#
